# Задание 1
# "Написать генератор нечётных чисел от 1 до n (включительно), используя ключевое слово yield
# С данным заданием сложностей не возникло совсем. Тема понятная. Как применять генераторы тоже в целом понятно"

# Задание 2 (со *)
# "Решить задачу генерации нечётных чисел от 1 до n (включительно), не используя ключевое слово yield"
# Так как с первым справился, с этим заданием вовсе не возникло никаких проблем

# Задание №3
# "Необходимо реализовать генератор, возвращающий кортежи вида (<tutor>, <klass>)"
# В данной задаче возникла сложность только с конечным выводом информации на экран. Пытался вывести все кортежи разом в столбик, а оказалось что можно было
# сделать через функцию next. С функцией next у меня получалось. Но задание видимо не до конца понял. В итоге усложнял себе жизнь. По итогу все получилось просто
#
# Задание №4
# "Представлен список чисел. Необходимо вывести те его элементы, значения которых больше предыдущего"
# Пробовал решить задачу посредством генератора. Честно говоря не полностью получилось. В итоге решил сделать по старинке через цикл for. 
#
# Задание №5
# "Представлен список чисел. Определить элементы списка, не имеющие повторений. Сформировать из этих элементов список с сохранением порядка их следования в исходном списке"
# В общем то воспользовавшись тем что проходили на лекцие по данной теме задачу удалось решть без проблем еще до следующей лекции. 
