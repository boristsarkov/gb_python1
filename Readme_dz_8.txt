# Задание №1
# Написать функцию email_parse(<email_address>), которая при помощи регулярного выражения извлекает имя пользователя и почтовый домен из email адреса и возвращает их в виде словаря. Если адрес не валиден, выбросить исключение ValueError.
# В целом задание оказалось не сложным. Сначала пошел по простому, но как оказалось не совсем правильному пути, хотел через разные переменные парсить имя и домен. Из-за этого не все матчилось. Потом сделал через группы в одну строку начало матчится. А дальше немного по возился с raise, никак он не выходил у меня на это условие. В итоге посоветовавшись с наставником он направил меня на путь истинный и все сразу получилось. 

# Задание №2 Со *
# Честно признаться просто уже не хватало времени в нем разбираться

# Задание №3
# Написать декоратор для логирования типов позиционных аргументов функции
# Честно признаться не полностью въехал в тему декораторов. Пока решил сделать задание по образу и подобию преподавателя. Разбираться буду еще с этой темой позже.

# Задание №4
# Написать декоратор с аргументом-функцией (callback), позволяющий валидировать входные значения функции и выбрасывать исключение ValueError, если что-то не так, например:
# Аналогично задаче №3
